server:
  port: ${PORT:8888}

spring:
  application:
    name: config-server
  cloud:
    config:
      server:
        git:
          uri: https://github.com/nsmtechdev/Be2.parcial
          default-label: main
          force-pull: true
          search-paths: config-data


